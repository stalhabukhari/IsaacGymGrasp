from setuptools import setup, find_packages

setup(
    name="isaacgymgrasp",
    version="0.1",
    packages=find_packages(include=["isaacgymgrasp", "isaacgymgrasp.*"]),
    # py_modules=[],
    # install_requires=[],
)
